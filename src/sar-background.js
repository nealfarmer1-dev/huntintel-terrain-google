import * as Location from "expo-location";import * as SecureStore from "expo-secure-store";import * as TaskManager from "expo-task-manager";import {publishSarPosition}from"./api";import{nextSarPosition}from"./sar-core";export const SAR_TASK="huntintel-terrain-live-sar";export const SAR_ACTIVE_KEY="terrain.liveSar.v1";if(!TaskManager.isTaskDefined(SAR_TASK))TaskManager.defineTask(SAR_TASK,async({data,error})=>{if(error)return;const active=JSON.parse((await SecureStore.getItemAsync(SAR_ACTIVE_KEY))||"null");if(!active?.sharingSessionId)return;for(const item of data?.locations||[]){try{await publishSarPosition(active.sessionId,nextSarPosition(item.coords,active.sharingSessionId,active.sequence++,"background"));await SecureStore.setItemAsync(SAR_ACTIVE_KEY,JSON.stringify(active))}catch{await SecureStore.deleteItemAsync(SAR_ACTIVE_KEY);await Location.stopLocationUpdatesAsync(SAR_TASK).catch(()=>{});break}}});export async function startSarBackground(value){const permission=await Location.requestBackgroundPermissionsAsync();if(permission.status!=="granted")throw new Error("Background location permission was not granted.");await SecureStore.setItemAsync(SAR_ACTIVE_KEY,JSON.stringify({...value,sequence:value.sequence||0}));await Location.startLocationUpdatesAsync(SAR_TASK,{accuracy:Location.Accuracy.High,timeInterval:15000,distanceInterval:15,showsBackgroundLocationIndicator:true,foregroundService:{notificationTitle:"HuntIntel live SAR sharing",notificationBody:"Your location is being shared with the authorized SAR team.",notificationColor:"#58764e"},pausesUpdatesAutomatically:true})}export async function stopSarBackground(){await SecureStore.deleteItemAsync(SAR_ACTIVE_KEY);if(await Location.hasStartedLocationUpdatesAsync(SAR_TASK))await Location.stopLocationUpdatesAsync(SAR_TASK)}
