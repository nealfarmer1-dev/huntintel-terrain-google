import assert from"node:assert/strict";import test from"node:test";import{DEFAULT_LAYER_PREFERENCES,breadcrumbLayerStatus,layerAvailability,normalizeLayerPreferences,offlineLayerPolicy,setBasemap,shouldApplyHydratedActiveBreadcrumb,shouldShowBreadcrumbEmptyState,toggleLayer}from"../src/map-layers.js";
test("exactly one supported basemap is persisted",()=>{assert.equal(setBasemap(DEFAULT_LAYER_PREFERENCES,"hybrid").basemap,"hybrid");assert.equal(normalizeLayerPreferences({basemap:"bad"}).basemap,"satellite");});
test("group toggles are immutable and availability does not fabricate data",()=>{const next=toggleLayer(DEFAULT_LAYER_PREFERENCES,"analysis","features");assert.equal(next.analysis.features,false);assert.equal(DEFAULT_LAYER_PREFERENCES.analysis.features,true);assert.equal(layerAvailability().team_positions,false);});
test("analysis boundary is always enabled even when persisted preferences try to hide it",()=>{assert.equal(normalizeLayerPreferences({analysis:{boundary:false}}).analysis.boundary,true);assert.equal(toggleLayer(DEFAULT_LAYER_PREFERENCES,"analysis","boundary").analysis.boundary,true);});
test("offline policy honors explicit provider permission",()=>{assert.equal(offlineLayerPolicy("boundary"),"package");assert.equal(offlineLayerPolicy("parcels",[{layers:[{type:"parcels",offlinePermitted:false}]}]),"online-only");assert.equal(offlineLayerPolicy("parcels",[{layers:[{type:"parcels",offlinePermitted:true}]}]),"provider-permitted");});
test("breadcrumb visibility status stays independent from recording",()=>{assert.equal(breadcrumbLayerStatus({visible:true,recording:true}),"Recording · Visible");assert.equal(breadcrumbLayerStatus({visible:false,recording:true}),"Recording · Hidden");assert.equal(breadcrumbLayerStatus({visible:true,hasRecordedPoints:true}),"Visible");assert.equal(breadcrumbLayerStatus({visible:false,hasRecordedPoints:true}),"Hidden");assert.equal(breadcrumbLayerStatus({visible:true}),"None recorded");});
test("breadcrumb empty state waits for reliable selected-analysis data",()=>{assert.equal(shouldShowBreadcrumbEmptyState({visible:true,hasSelectedAnalysis:true}),true);assert.equal(shouldShowBreadcrumbEmptyState({visible:true,loading:true,hasSelectedAnalysis:true}),false);assert.equal(shouldShowBreadcrumbEmptyState({visible:true,unavailable:true,hasSelectedAnalysis:true}),false);assert.equal(shouldShowBreadcrumbEmptyState({visible:true,recording:true,hasSelectedAnalysis:true}),false);assert.equal(shouldShowBreadcrumbEmptyState({visible:true,hasSelectedAnalysis:false}),false);assert.equal(breadcrumbLayerStatus({visible:true,unavailable:true}),"Trail status unavailable");});
test("hydrated active state applies only to the latest unchanged presentation revision",()=>{assert.equal(shouldApplyHydratedActiveBreadcrumb({requestId:2,currentRequestId:2,startRevision:4,currentRevision:4}),true);assert.equal(shouldApplyHydratedActiveBreadcrumb({requestId:1,currentRequestId:2,startRevision:4,currentRevision:4}),false);assert.equal(shouldApplyHydratedActiveBreadcrumb({requestId:2,currentRequestId:2,startRevision:4,currentRevision:5}),false);});
