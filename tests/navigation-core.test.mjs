import assert from"node:assert/strict";import test from"node:test";import{appendBreadcrumbPoint,bearingDegrees,createBreadcrumb,distanceMeters,navigationSnapshot,transitionBreadcrumb}from"../src/navigation-core.js";
test("distance and bearing use deterministic fixtures",()=>{const from={latitude:32,longitude:-86},to={latitude:32.001,longitude:-86};assert.ok(Math.abs(distanceMeters(from,to)-111.2)<0.5);assert.ok(Math.abs(bearingDegrees(from,to))<0.01);});
test("arrival and stale state are explicit",()=>{const result=navigationSnapshot({latitude:32,longitude:-86,recordedAt:"2026-01-01T00:00:00Z"},{latitude:32,longitude:-86},{arrivalRadiusMeters:20,now:Date.parse("2026-01-01T00:01:00Z")});assert.equal(result.arrived,true);assert.equal(result.stale,true);});
test("breadcrumb survives pause resume finish rename and ignores paused points",()=>{let track=createBreadcrumb({id:"x",startedAt:"2026-01-01T00:00:00Z"});track=appendBreadcrumbPoint(track,{clientPointId:"a",recordedAt:"2026-01-01T00:00:01Z"});track=transitionBreadcrumb(track,"pause");track=appendBreadcrumbPoint(track,{clientPointId:"b",recordedAt:"2026-01-01T00:00:02Z"});assert.equal(track.points.length,1);track=transitionBreadcrumb(track,"rename",{name:"North ridge"});track=transitionBreadcrumb(track,"resume");track=transitionBreadcrumb(track,"finish");assert.equal(track.name,"North ridge");assert.equal(track.status,"finished");});
