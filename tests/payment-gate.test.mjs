import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";
test("Android setup quotes before Analyze and native payment uses only server entitlement routes",async()=>{const[app,gate,api]=await Promise.all([readFile(new URL("../App.tsx",import.meta.url),"utf8"),readFile(new URL("../src/PaymentGate.tsx",import.meta.url),"utf8"),readFile(new URL("../src/api.js",import.meta.url),"utf8")]);for(const value of["Confirm Acreage & Price","setupPhase","MAX_ACRES = 2000"])assert.match(app,new RegExp(value.replace(/[?.]/g,"\\$&")));for(const value of["Complete Test Payment","Pending","Fail","Cancel","Timeout","Retry Analysis — No Repurchase","Restore / Reconcile"])assert.match(gate,new RegExp(value));assert.match(gate,/submitPaidAnalysis/);assert.doesNotMatch(gate,/Play Billing|purchaseToken|transactionReceipt/);for(const route of["analysis-drafts","payment-attempts","verify","reconcile","analyze"])assert.match(api,new RegExp(route));});
