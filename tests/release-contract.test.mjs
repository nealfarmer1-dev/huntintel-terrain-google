import assert from "node:assert/strict";import test from "node:test";import{readFile}from"node:fs/promises";
test("Android API propagates correlation IDs and exposes safe retry metadata",async()=>{const source=await readFile(new URL("../src/api.js",import.meta.url),"utf8"),offline=await readFile(new URL("../src/offline.js",import.meta.url),"utf8");assert.match(source,/X-Correlation-ID/);assert.match(source,/error\.correlationId/);assert.match(source,/Retry-After/);assert.match(source,/fetchFeatureFlags/);assert.match(source,/clearSession/);assert.match(offline,/ACCOUNT_ACCESS_REVOKED/);assert.match(offline,/removeOfflinePackage/);assert.doesNotMatch(source,/POSTMARK_SERVER_TOKEN|AUTH_TOKEN_SECRET|DB_PASSWORD/)});
