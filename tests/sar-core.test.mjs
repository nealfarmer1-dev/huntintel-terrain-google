import assert from"node:assert/strict";import test from"node:test";import{readFile}from"node:fs/promises";test("Android SAR is explicit, visibly stoppable, and background permission is separate",async()=>{const ui=await readFile(new URL("../src/SarScreen.tsx",import.meta.url),"utf8"),bg=await readFile(new URL("../src/sar-background.js",import.meta.url),"utf8");assert.match(ui,/LIVE SHARING ACTIVE/);assert.match(ui,/Stop sharing/);assert.match(bg,/requestBackgroundPermissionsAsync/);assert.match(bg,/foregroundService/);assert.doesNotMatch(bg,/queueOfflineOperation/)});
