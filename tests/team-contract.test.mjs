import assert from"node:assert/strict";import test from"node:test";import{readFile}from"node:fs/promises";test("Android exposes team workflows without raw analysis-ID entry",async()=>{const api=await readFile(new URL("../src/api.js",import.meta.url),"utf8"),ui=await readFile(new URL("../src/TeamsScreen.tsx",import.meta.url),"utf8");assert.match(api,/team-invitations/);assert.match(api,/\/members/);assert.match(api,/\/analyses/);assert.match(ui,/Share a saved analysis/);assert.doesNotMatch(ui,/placeholder="Owned analysis ID"/) });
