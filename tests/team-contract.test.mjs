import assert from"node:assert/strict";import test from"node:test";import{readFile}from"node:fs/promises";test("Android exposes Section 7 team workflows and excludes live SAR",async()=>{const api=await readFile(new URL("../src/api.js",import.meta.url),"utf8"),ui=await readFile(new URL("../src/TeamsScreen.tsx",import.meta.url),"utf8");assert.match(api,/team-invitations/);assert.match(api,/\/members/);assert.match(api,/\/analyses/);assert.match(ui,/Build Section 8/);assert.doesNotMatch(ui,/startLive|publishLocation/) });
